<nav>
  <div class="nav__brand">
    <div class="nav__logo"></div>
    <div class="nav__title">Today's Deep Learning</div>
  </div>
  <div class="nav__user">
    <% if current_user %>
      Welcome, <%= current_user.email %> | <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
    <% else %>
      Welcome, <%= link_to "Sign Up", new_user_registration_path %>
    <% end %>
  </div>
</nav>
